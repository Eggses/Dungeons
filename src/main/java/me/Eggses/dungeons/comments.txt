
        /*
        maybe there is a way to cancel mob dros
         */

        /*
        entity.remove();??? maybe on boss fail call thsu.
        entity.launchProjectile() wtf is this

        this is the class remove the other Dungeon Mob under attempt.
         */
        /*
        that Knagroo guy had stuff on regions, owrking out if a player
        was in a region...

        its unfornautly a linear check but what can you do.
         */


         Make sure removeMob is called on mob death / despawn / chunk unload,
         otherwise your repeating tasks will happily keep running holding references to old entities.

         What if someone leaves, then reenters the region... maybe add checks to each thing
         like if no player is within 20 blocks return...

         but then tasks infinaitely run... maybe on chunk unload do something idk but then
         how what if people die and run back...



        /*

        Map<DungeonZoneID_ENUM, Code that spawns all the mobs, some 90 -> {// mobs}
        Then, have

        Map<Region, DungeonZoneID_ENUM>, and basically, get the key set, find if a player is in any of the regions
        on a move event, only issue is linear so might need to work somehting out., gets the key, pull the
        code, and run

        and you can also do like on chest open, or on button press, all the code etc for the area...


         */